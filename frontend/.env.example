# Retell AI Configuration (Frontend - Required)
# Get these from your Retell AI Dashboard: https://dashboard.retellai.com
VITE_RETELL_PUBLIC_KEY=public_key_bf22fb5e1e7bf6743cc5b
VITE_RETELL_AGENT_ID=agent_4656c1fe4b5b11b7602885338e

# Optional: reCAPTCHA v3 for bot protection (if enabled in Retell settings)
# VITE_RECAPTCHA_SITE_KEY=your_recaptcha_site_key_here

# SMTP Configuration for Vercel Serverless Functions (api/contact.js)
# NOTE: These are NOT prefixed with VITE_ because they're server-side only
# Add these to Vercel Environment Variables Dashboard

# SMTP Server (from Namecheap cPanel)
SMTP_HOST=mail.cognidrift.com

# SMTP Port (465 for SSL, 587 for TLS)
SMTP_PORT=465

# Set to "true" for port 465 (SSL), "false" for port 587 (TLS)
SMTP_SECURE=true

# Your full email address
SMTP_USER=contact@cognidrift.com

# Your email password from cPanel
SMTP_PASS=YourPasswordHere

# Display name for outgoing emails
SMTP_FROM_NAME=CogniDrift

# Your email address (usually same as SMTP_USER)
SMTP_FROM_EMAIL=contact@cognidrift.com
